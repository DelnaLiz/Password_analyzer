# Password_analyzer
Checks passwords against two policy models, traditional complexity rules and NIST 800-63B length-based rules ,then scores real-world strength with zxcvbn against a naive entropy estimate, showing how complexity rules overestimate security for predictable passwords. Includes a cryptographically random password generator for comparison.
